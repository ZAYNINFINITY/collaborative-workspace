NODE_ENV=development
PORT=5000

# Local development defaults (do not reuse these values in production).
# Production config should be set in Railway/Vercel environment variables.

# MongoDB
# Replace YOUR_DB_PASS with your rotated password (URL-encode if it contains special characters).
MONGO_URI=mongodb+srv://ZAYN:YOUR_DB_PASS@clustercollab.4zlakf0.mongodb.net/collaborative-workspace?retryWrites=true&w=majority&appName=ClusterCollab

# Secrets (use strong random strings in production)
JWT_SECRET=dev_jwt_secret_change_me
SESSION_SECRET=dev_session_secret_change_me

# CORS / URLs
CLIENT_URL=https://collaborative-workspace-rosy.vercel.app
SERVER_URL=http://localhost:5000
OAUTH_PUBLIC_BASE_URL=https://collaborative-workspace-rosy.vercel.app

# OAuth (optional for local dev; register real callbacks for production)
GITHUB_CLIENT_ID=Ov23li6ITDEog0CrW4oZ
GITHUB_CLIENT_SECRET=4b892106673d92172e3facdad4a0c4511bdfa3e2
GITHUB_CALLBACK_URL=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=http://localhost:5000/api/auth/google/callback

# Email (optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
